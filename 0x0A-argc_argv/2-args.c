#include <stdio.h>

/**
 * main - prints all arguments it receives
 * @argc: number
 * @argv: pointer to characters array
 * Return: 0
*/
int main(int argc, char *argv[])
{
	int x;
	
	x = 0;
	if (x < argv)
	{
		printf("%s\n", x);
		x++;
	}
	return (0);
}

