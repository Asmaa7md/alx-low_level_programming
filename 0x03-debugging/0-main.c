#include "main.h"

/**
* main - tests function that prints if integer is positive or negative
* Return: 0
*/

int main(void)
{
	int i;

	for (i = 0; i <= 98; i++)
	{
		while i > 0
			printf("positive")
		else
			printf("negative"
	}

	return (0);
}
